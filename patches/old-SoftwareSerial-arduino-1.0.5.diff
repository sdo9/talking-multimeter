--- /usr/share/arduino/libraries/SoftwareSerial/SoftwareSerial.cpp	2014-02-16 23:54:56.000000000 -0500
+++ src/SoftwareSerial.cpp	2015-10-17 16:25:39.525066650 -0400
@@ -301,19 +301,23 @@
   }
 }
 
+#if 0
 #if defined(PCINT0_vect)
 ISR(PCINT0_vect)
 {
   SoftwareSerial::handle_interrupt();
 }
 #endif
+#endif
 
+#if 0
 #if defined(PCINT1_vect)
 ISR(PCINT1_vect)
 {
   SoftwareSerial::handle_interrupt();
 }
 #endif
+#endif
 
 #if defined(PCINT2_vect)
 ISR(PCINT2_vect)
@@ -322,12 +326,14 @@
 }
 #endif
 
+#if 0
 #if defined(PCINT3_vect)
 ISR(PCINT3_vect)
 {
   SoftwareSerial::handle_interrupt();
 }
 #endif
+#endif
 
 //
 // Constructor
@@ -354,8 +360,8 @@
 
 void SoftwareSerial::setTX(uint8_t tx)
 {
-  pinMode(tx, OUTPUT);
-  digitalWrite(tx, HIGH);
+  //pinMode(tx, OUTPUT);
+  //digitalWrite(tx, HIGH);
   _transmitBitMask = digitalPinToBitMask(tx);
   uint8_t port = digitalPinToPort(tx);
   _transmitPortRegister = portOutputRegister(port);
